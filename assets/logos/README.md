# Competitor logos (South Africa ranking)

Drop PNG or JPG files here to show competitor logos in the dashboard ranking section.

**Suggested filenames** (lowercase, no spaces; set `logo_path` in `dashboard.py` to match):

- `payflex.png`
- `payjustnow.png`
- `moretyme.png`
- `mobicred.png`
- `happypay.png` (or `happy_pay.png`)
- `float.png`

In `dashboard.py`, set each competitor’s `logo_path` to `"assets/logos/<filename>"` (e.g. `"assets/logos/payflex.png"`). If the file is missing, the dashboard shows initials instead.
